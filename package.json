{
  "name": "hyev",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3002",
    "dev:local": "next dev -p 3002",
    "dev:turbopack": "next dev --turbopack -p 3002",
    "dev:reset": "powershell -ExecutionPolicy Bypass -File scripts/dev.ps1",
    "clean": "node -e \"try{require('fs').rmSync('.next',{recursive:true,force:true})}catch(e){}\"",
    "build": "prisma generate && next build",
    "start": "next start -p 3002",
    "lint": "next lint",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:studio": "prisma studio",
    "deploy:prepare": "powershell -ExecutionPolicy Bypass -File scripts/deploy-prepare.ps1",
    "deploy:rules": "cross-env NODE_OPTIONS=--use-system-ca firebase deploy --only firestore:rules --non-interactive",
    "deploy:indexes": "cross-env NODE_OPTIONS=--use-system-ca firebase deploy --only firestore:indexes --non-interactive",
    "deploy:firestore": "cross-env NODE_OPTIONS=--use-system-ca firebase deploy --only firestore --non-interactive",
    "deploy:storage": "cross-env NODE_OPTIONS=--use-system-ca firebase deploy --only storage --non-interactive",
    "deploy:all": "cross-env NODE_OPTIONS=--use-system-ca firebase deploy --only firestore,storage --non-interactive",
    "apphosting:sync-env": "powershell -ExecutionPolicy Bypass -File scripts/sync-apphosting-env.ps1",
    "db:migrate-firestore": "node scripts/migrate-sqlite-to-firestore.mjs"
  },
  "dependencies": {
    "@prisma/client": "^6.8.2",
    "firebase": "^11.6.0",
    "firebase-admin": "^13.10.0",
    "next": "^15.3.2",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@types/node": "^22",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "autoprefixer": "^10.4.21",
    "cross-env": "^7.0.3",
    "eslint": "^9",
    "eslint-config-next": "^15.3.2",
    "firebase-tools": "^14.0.0",
    "postcss": "^8.5.3",
    "prisma": "^6.8.2",
    "tailwindcss": "^3.4.17",
    "typescript": "^5"
  }
}
