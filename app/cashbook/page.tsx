import { CashbookClient } from "./CashbookClient";

export const metadata = { title: "สมุดเงินสด — HYEV" };
export const dynamic = "force-dynamic";

export default function CashbookPage() {
  return <CashbookClient userName="" />;
}
