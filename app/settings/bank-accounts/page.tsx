import { BankAccountsSettingsClient } from "./BankAccountsSettingsClient";

export const metadata = { title: "ตั้งค่าบัญชีธนาคาร — HYEV" };

export default function BankAccountsSettingsPage() {
  return <BankAccountsSettingsClient />;
}
